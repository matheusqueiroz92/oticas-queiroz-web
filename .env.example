# =========================================
# Variáveis globais do monorepo
# Copie este arquivo para .env (apenas para uso de scripts globais).
# Para o app Next, use apps/web/.env.local
# =========================================

NODE_ENV=development
